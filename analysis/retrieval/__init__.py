"""The retrieval unit -- the five-column chunk contract and BM25 lexical search.

The rules are carried over from
analysis/slices/ydc/{trend,chunks,topics,bm25,retrieval_eval,encode_chunks,hybrid}.py (ydc `v0.1.0`
`02440ab`) -- which file each piece came from is the parenthesis in each module header. The slices were
reference copies and were never imported, and once everything to carry over had been carried, fork #9 deleted
that directory. The one place to see the original again is that tag in the ydc repo.
"""
