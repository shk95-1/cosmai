"""검색 유닛 — 5컬럼 청크 계약과 BM25 어휘 검색.

규칙은 analysis/slices/ydc/{trend,chunks,topics,bm25,retrieval_eval,encode_chunks,hybrid}.py
(ydc `v0.1.0` `02440ab`) 를 옮긴 것이다 — 파일별 자리는 각 모듈 헤더의 괄호다. 슬라이스는 참조용
사본이라 import 하지 않았고, 옮길 것을 다 옮긴 뒤 포크 #9 가 그 디렉터리를 지웠다. 원본을 다시 볼
자리는 ydc 레포의 그 태그 하나다.
"""
