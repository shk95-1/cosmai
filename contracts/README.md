# contracts — 기계가 검사할 수 있는 것만

이 디렉터리는 설계서가 아니다. 여기 있는 것은 전부 **테스트나 스크립트가 대조할 수 있는 형태**여야 한다.

| 파일 | 내용 | 검사 방법 |
|---|---|---|
| `ddl/current/*.sql` | 2026-08-23 현재 DB의 실제 스키마 덤프 (app.trend_radar 13 · app.tubedepth 13 · cosmai.cosmai 12 테이블). 수집기 이식 후에도 **이 테이블 모양을 유지**해야 한다 | `pg_dump --schema-only` 재덤프 → diff |
| `ddl/needs/001_needs.sql` | 신규 `app.needs` 스키마 (17 테이블). 주석 3곳은 낡았다 — 아래 각주 | 마이그레이션 적용 후 `pg_catalog` 대조 테스트 |
| `ddl/needs/002_audit_additive.sql` | 2026-08-23 계약 감사(이슈 #17)가 요구한 **추가만** — 테이블 3(`need_key`, `category_map`, `product_ref_candidate`) + 컬럼 31. 합계 20 테이블 | 같은 테스트 + `tests/test_ddl_additive_only.py` |
| `ddl/needs/005_need_mention_natural_key.sql` | `need_mention` 자연키 교체 — btree 2704B 상한(#5 운영 실패)과 시드·분석 충돌(#12 안 A)을 한 번에 푼다. `UNIQUE (src, ref, need_key, sentence)` → `UNIQUE INDEX (src, ref, need_key, extractor_version, md5(sentence))`. **추가만의 유일한 예외**(사용자 승인 2026-08-24) | `tests/test_ddl_additive_only.py` 의 `SANCTIONED_DESTRUCTIVE` 화이트리스트 + `tests/test_need_mention_natural_key.py` |
| `ddl/needs/020_retrieval_chunk.sql` | 검색 유닛(#28)의 청크 저장소 `needs.retrieval_chunk` — 5칸 청크 계약(`analysis/retrieval/chunks.py` FIELDS) + 파생 2칸(`text_md5`·`chunked_at`). 번호 020 은 장수 브랜치 `feat/ydc-import` 의 블록이라 main 의 00N 과 파일명이 겹치지 않는다 | `tests/retrieval/test_contract.py` (컬럼·NOT NULL·GRANT·번호대) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/021_aspect_lexicon_extra.sql` | `aspect_lexicon.extra`(jsonb) — 룰셋마다 다른 사실을 담는 칸. 검색 유닛의 주제 사전(`ruleset='retrieval-topic'`, 포크 #8)이 별칭의 표기 계열·주제 유형·트렌드 사용 여부를 여기 싣는다. 기존 행은 `{}` 로 남는다(추가만) | `tests/retrieval/test_topics.py` (적재·활성·동등성) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/022_panel_and_quarter.sql` | 패널 모집단 `needs.panel_roster`+`needs.panel_channel`(43채널 명부의 자리 — 값은 `eval/panel/channels_v1.csv` 를 `db/seed/panel.py` 가 넣는다, 포크 #31)과 분기 입자의 정본 `needs.metrics_topic_quarter`(월과 **공존**한다; 어느 표가 어느 입자를 지는지는 `formats.md` §시간). 두 표 다 어떤 모집단에 대한 비율인지를 행에 남기고, 그 포인터는 판본 표를 FK 로 가리킨다. 주제 축은 `topic_key` 다 — 레지스트리가 `aspect_lexicon(ruleset='retrieval-topic')` 이라 `needs.need_key` 와 다른 축이다. 행 집합의 두 불변식(조밀한 격자 · 닫힌 분모)은 뷰 `db/views/metrics_topic_quarter_violation.sql` 이 되묻는다. 값은 `analysis/trend`(`cosmai trend quarter`, 포크 #5)가 활성 코퍼스 스냅샷에서 넣는다 | `tests/test_panel_quarter_contract.py` (정본·명부·분모·역할 어휘·`quarter` 문법·두 축·자리수·불변식 뷰) + `tests/test_trend_quarter.py` (다섯 수식) + `tests/test_trend_pipeline.py` (모집단·적재 뒤 뷰가 빈다) + `tests/test_trend_golden.py` (ydc `analysis/slices/ydc/trend.py` 와 1:1) + `tests/test_month_grain_regression.py` (월 산출 불변) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/023_corpus_snapshot.sql` | 2026-08-19 유튜브 코퍼스 스냅샷의 자리 `needs.corpus_snapshot`+`corpus_document`+`corpus_mention`(포크 #4). ydc 인계 CSV 세 장의 261,317 문서·105,358 언급이 여기 산다 — 값은 `archive/yt-handoff/` 를 `db/corpus`(`python -m db.corpus load <dir>`)가 넣는다. **관측 판본이 유일키 맨 앞에 서서** 재수집(#38)이 같은 `source + source_item_id` 로 얹혀도 옛 관측을 덮지 못한다. 채널은 표가 되지 않고 `panel_channel` 활성 명부와 대조만 된다(분모는 한 표에만 산다). 규칙·한계 문장의 자리는 `formats.md` §코퍼스 스냅샷 · `interfaces.md` §모집단의 한계이고, 매니페스트가 선언한 행수는 적재기가 반입분과 대조해 다르면 켜기 전에 거절한다 — 새 판본이면 삽입 행수까지 맞대어 `input_counts.duplicate_docs = 0` 도 함께 선다(포크 #37) | `tests/test_corpus_import.py` (반입·멱등·덮임 없음·재현 숫자 셋·선언 행수·중복 유일키·계약 문장) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/024_topic_quarter_judgement.sql` | 트렌드 유형 7종 판정과 두 점수의 자리 `needs.topic_quarter_judgement`(포크 #40). ydc `judge.py` 승격분이고 `metrics_topic_quarter` 의 기본키 여덟 칸이 그대로 이 표의 키이자 FK 다 — **집계가 아니라 파생**이라 이름이 `metrics_` 로 시작하지 않고 `formats.md` §시간 의 그레인 표에 줄을 갖지 않는다. 판정 상수 다섯의 근거와 채택/재적합 판단은 `interfaces.md` §판정 의 표가 지고, 값은 `analysis/judge`(`cosmai trend judge`)가 넣는다. 지표 행과의 1:1 과 `gap_pp` 의 두 행 일치는 뷰 `db/views/topic_quarter_judgement_violation.sql` 이 되묻는다 | `tests/test_judgement_contract.py` (파생·어휘·자리수·닫힌 사유·불변식 뷰) + `tests/test_judge_constants.py` (상수가 계약 표와 같은 수) + `tests/test_judge_rules.py` (판정 순서와 두 점수) + `tests/test_judge_golden.py` (ydc `analysis/slices/ydc/judge.py` 와 1:1) + `tests/test_judge_pipeline.py` (읽기·적재·멱등) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/025_topic_quarter_evidence.sql` | 판정 셀을 받치는 소비자 발화의 자리 `needs.topic_quarter_evidence`(포크 #6). ydc `evidence_comments.py` 승격분이고 `topic_quarter_judgement` 의 기본키 여덟 칸이 그대로 이 표의 키이자 FK 다 — 판정이 지표 행에서 행 하나를 만드는 **파생**이라면 근거는 그 셀을 만든 문서를 도로 가리키는 **포인터**라, 본문을 베끼지 않고 `corpus_document` 를 FK 로 든다. 셀에서 원문까지 손으로 조인하지 않고 닿는 길은 뷰 `db/views/topic_quarter_evidence_quote.sql` 이고(이 이슈의 완료 기준), 근거가 정말 그 셀의 것인지는 `db/views/topic_quarter_evidence_violation.sql` 이 되묻는다. 선별 규칙 넷과 `cosmai retrieval search` 로 대체하지 않는 근거(실측 포함)는 `interfaces.md` §근거 가 지고, 값은 `analysis/evidence`(`cosmai trend evidence`)가 넣는다. 그 위에서 규칙이 유형을 정하는 기회 카드(`cosmai trend cards`, ydc `cards.py`)는 **표를 만들지 않는다** — 같은 계약의 §기회 카드 | `tests/test_evidence_contract.py` (포인터·모집단 동치·자리 사다리·불변식 뷰) + `tests/test_evidence_rules.py` (선별 규칙 넷) + `tests/test_evidence_pipeline.py` (읽기·적재·멱등) + `tests/test_cards_rules.py` (유형 배정과 어휘 여섯) + `tests/test_evidence_numbers.py` (계약이 인용한 픽스처 수치와 `tool/measure-evidence-fixture` 의 재측정·DB 산출 삼자 대조) + `tests/test_evidence_golden.py` (ydc `evidence_comments.py`·`cards.py` 와 대조) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/*.sql` | (위와 같은 디렉터리) | 적용 경로는 `db/migrate.sh` 하나 (운영·테스트 공용); 적용 여부는 원장 `needs.schema_migration`에 기록 |
| `interfaces.md` §민감도 | 민감도·후향 검증 셋(포크 #41)의 세 반사실 모집단과 상수. ydc `panel_sensitivity.py`·`backtest.py`·`spam_ad_flags.py` 승격분이고 **어느 표에도 쓰지 않는다** — 반사실 행에는 022 의 `panel_role` 어휘에도 `analysis_run` 에도 자리가 없어서다. 답은 `cosmai trend sensitivity` 의 stdout 이다 | `tests/test_sensitivity_rules.py` (창 유도·판정 대상 게이트·세 적중 기준·버린 정규식·묶음 단위 제외) + `tests/test_sensitivity_golden.py` (ydc 세 스크립트와 1:1) + `tests/test_sensitivity_pipeline.py` (막힘·종료 코드·저장된 표가 그대로인가) |
| `interfaces.md` §대조 | 네 소스(유튜브 댓글·자막·커머스 리뷰·커머스 랭킹)를 나란히 놓는 세 답과 상수. ydc `source_composition.py`·`commerce_crosscheck.py`·`cross_source.py` 승격분이고 **어느 표에도 쓰지 않는다** — 이 답의 행은 (주제)나 (성분) 하나가 키인데 022 의 분기 입자는 여덟 칸이 키이고 커머스 쪽에는 분기도 명부도 없다. 그래서 이 승격의 DDL 은 0장이다. 답은 `cosmai trend crosscheck` 의 stdout 이다. 같은 절이 성분 키 오매칭 정정(`시카`→트라이에톡시카프릴릴실레인)과 `FORMULA_HOLD`·`PAPER_HOLD` 의 근거를 진다. ydc `commerce_ranking.py` 는 `analysis/aggregate/ranking.py` 와 겹쳐 승격하지 않는다(처분 보류) | `tests/test_crosscheck_rules.py` (구성 정의·극성·긍정률·해석 다섯) + `tests/test_crosscheck_keys.py` (고친 키·버린 별칭 셋·성분표 쪼개기) + `tests/test_crosscheck_pipeline.py` (읽기·막힘·종료 코드·아무것도 안 쓴다) + `tests/test_crosscheck_golden.py` (ydc 세 스크립트가 못 박은 입력·출력과 1:1) |
| `entrypoints.md` | CLI 진입점·종료 코드·run/fetch_log 공통 뷰 | CLI `--help` 스냅샷 + 뷰 존재 테스트 |
| `interfaces.md` | 분석 패키지 4개의 입출력 타입·수식, 평가 하네스가 넘어야 할 기준선 | `tests/test_contract_types.py` 가 `analysis/types.py` 와 대조 · `eval <task>` 가 기준선과 비교 |
| `formats.md` | 사전·평가셋 CSV/테이블 포맷 | 적재 스크립트가 검증 |
| `secrets.md` | secret 파일 경로와 키 이름 (값 없음) | 기동 시 키 존재 검사 |
| `versioning.md` | `*_version` 컬럼 규칙과 `analysis_run.versions` 의 키 | `tests/test_version_strings.py` (두 형식) + `tests/test_trend_quarter.py` (`metric` 키) + 코드 리뷰 |
| `../db/grants/needs_runtime_reader.sql` | 분석이 읽는 원천 테이블 12개의 SELECT 권한 | `tests/test_grants_reader.py` 가 `ddl/current` 와 대조 |

원칙: 인터페이스는 여기서 먼저 고정하고, 동작은 패키지 안에서 점진 구현·검증한다. 계약 변경은 PR 한 개에 계약+구현+테스트가 같이 온다.

## 001 주석 정정 (001 은 수정하지 않는다 — 추가만이 사전 승인 범위다)
- `001:8` "needs_reader: SELECT" — 그런 롤은 없다(T12). 읽기는 PostgREST `postgrest_anon` 화이트리스트(`db/grants/postgrest_anon_needs.sql`)와 분석용 `needs_runtime`(`db/grants/needs_runtime_reader.sql`) 둘뿐이다.
- `001:74` `labeled_set.task` 주석의 `aspect` — 평가셋도 기준선도 없어 뺐다(B11). 현행 목록은 `entrypoints.md`.
- `001:156` `low_complete` 주석 "RATING_ASC 표본에 3★이 섞임" — 실제 규칙은 `(low_collected < 150) or has_3star` 다(T5). `formats.md §표본 상수`.
- `001:105` `UNIQUE (src, ref, need_key, sentence)` — 005 가 갈아엎어 이제 거짓이다. 현행 자연키는 `UNIQUE INDEX (src, ref, need_key, extractor_version, md5(sentence))`. `ddl/needs/005_need_mention_natural_key.sql`.
