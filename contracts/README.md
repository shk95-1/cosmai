# contracts — 기계가 검사할 수 있는 것만

This directory is not a design document. Everything here has to be in **a shape a test or a script can compare against**.

| file | content | how it is checked |
|---|---|---|
| `ddl/current/*.sql` | the actual schema dump of the DB as of 2026-08-23 (app.trend_radar 13 · app.tubedepth 13 · cosmai.cosmai 12 tables). **These table shapes must hold** even after the collectors are ported | re-dump with `pg_dump --schema-only` → diff |
| `ddl/needs/001_needs.sql` | the new `app.needs` schema (17 tables). Three comments are stale — see the footnote below | a `pg_catalog` comparison test after the migration is applied |
| `ddl/needs/002_audit_additive.sql` | the **additions only** the 2026-08-23 contract audit (issue #17) asked for — 3 tables (`need_key`, `category_map`, `product_ref_candidate`) + 31 columns. 20 tables in total | the same test + `tests/test_ddl_additive_only.py` |
| `ddl/needs/005_need_mention_natural_key.sql` | the `need_mention` natural-key replacement — it solves the btree 2704B ceiling (#5, a production failure) and the seed/analysis collision (#12 option A) at once. `UNIQUE (src, ref, need_key, sentence)` → `UNIQUE INDEX (src, ref, need_key, extractor_version, md5(sentence))`. **The one exception to additive-only** (user approval 2026-08-24) | the `SANCTIONED_DESTRUCTIVE` whitelist in `tests/test_ddl_additive_only.py` + `tests/test_need_mention_natural_key.py` |
| `ddl/needs/020_retrieval_chunk.sql` | the retrieval unit's (#28) chunk store `needs.retrieval_chunk` — the 5-column chunk contract (`analysis/retrieval/chunks.py` FIELDS) + 2 derived columns (`text_md5`·`chunked_at`). The number 020 is the long-lived branch `feat/ydc-import`'s block, so file names never collide with main's 00N | `tests/retrieval/test_contract.py` (columns · NOT NULL · GRANT · number block) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/021_aspect_lexicon_extra.sql` | `aspect_lexicon.extra` (jsonb) — the column holding facts that differ per ruleset. The retrieval unit's topic dictionary (`ruleset='retrieval-topic'`, fork #8) carries an alias's script family, the topic type and whether it is used in trends here. Existing rows stay `{}` (additive only) | `tests/retrieval/test_topics.py` (load · active · equivalence) + `tests/retrieval/test_lexicon_v3.py` (the v3 ledger against the loaded source) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/022_panel_and_quarter.sql` | 패널 모집단 `needs.panel_roster`+`needs.panel_channel`(43채널 명부의 자리 — 값은 `eval/panel/channels_v1.csv` 를 `db/seed/panel.py` 가 넣는다, 포크 #31)과 분기 입자의 정본 `needs.metrics_topic_quarter`(월과 **공존**한다; 어느 표가 어느 입자를 지는지는 `formats.md` §시간). 두 표 다 어떤 모집단에 대한 비율인지를 행에 남기고, 그 포인터는 판본 표를 FK 로 가리킨다. 주제 축은 `topic_key` 다 — 레지스트리가 `aspect_lexicon(ruleset='retrieval-topic')` 이라 `needs.need_key` 와 다른 축이다. 행 집합의 두 불변식(조밀한 격자 · 닫힌 분모)은 뷰 `db/views/metrics_topic_quarter_violation.sql` 이 되묻는다. 값은 `analysis/trend`(`cosmai trend quarter`, 포크 #5)가 활성 코퍼스 스냅샷에서 넣는다 | `tests/test_panel_quarter_contract.py` (정본·명부·분모·역할 어휘·`quarter` 문법·두 축·자리수·불변식 뷰) + `tests/test_trend_quarter.py` (다섯 수식) + `tests/test_trend_pipeline.py` (모집단·적재 뒤 뷰가 빈다) + `tests/test_trend_golden.py` (ydc v0.2 trend.py 산출과 1:1) + `tests/test_month_grain_regression.py` (월 산출 불변) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/023_corpus_snapshot.sql` | 2026-08-19 유튜브 코퍼스 스냅샷의 자리 `needs.corpus_snapshot`+`corpus_document`+`corpus_mention`(포크 #4). ydc 인계 CSV 세 장의 261,317 문서·105,358 언급이 여기 산다 — 값은 `archive/yt-handoff/` 를 `db/corpus`(`python -m db.corpus load <dir>`)가 넣는다. **관측 판본이 유일키 맨 앞에 서서** 재수집(#38)이 같은 `source + source_item_id` 로 얹혀도 옛 관측을 덮지 못한다. 채널은 표가 되지 않고 `panel_channel` 활성 명부와 대조만 된다(분모는 한 표에만 산다). 규칙·한계 문장의 자리는 `formats.md` §코퍼스 스냅샷 · `interfaces.md` §모집단의 한계이고, 매니페스트가 선언한 행수는 적재기가 반입분과 대조해 다르면 켜기 전에 거절한다 — 새 판본이면 삽입 행수까지 맞대어 `input_counts.duplicate_docs = 0` 도 함께 선다(포크 #37) | `tests/test_corpus_import.py` (반입·멱등·덮임 없음·재현 숫자 셋·선언 행수·중복 유일키·계약 문장) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/024_topic_quarter_judgement.sql` | 트렌드 유형 7종 판정과 두 점수의 자리 `needs.topic_quarter_judgement`(포크 #40). ydc `judge.py` 승격분이고 `metrics_topic_quarter` 의 기본키 여덟 칸이 그대로 이 표의 키이자 FK 다 — **집계가 아니라 파생**이라 이름이 `metrics_` 로 시작하지 않고 `formats.md` §시간 의 그레인 표에 줄을 갖지 않는다. 판정 상수 다섯의 근거와 채택/재적합 판단은 `interfaces.md` §판정 의 표가 지고, 값은 `analysis/judge`(`cosmai trend judge`)가 넣는다. 지표 행과의 1:1 과 `gap_pp` 의 두 행 일치는 뷰 `db/views/topic_quarter_judgement_violation.sql` 이 되묻는다 | `tests/test_judgement_contract.py` (파생·어휘·자리수·닫힌 사유·불변식 뷰) + `tests/test_judge_constants.py` (상수가 계약 표와 같은 수) + `tests/test_judge_rules.py` (판정 순서와 두 점수) + `tests/test_judge_golden.py` (ydc v0.2 judge.py 산출과 1:1) + `tests/test_judge_pipeline.py` (읽기·적재·멱등) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/025_topic_quarter_evidence.sql` | 판정 셀을 받치는 소비자 발화의 자리 `needs.topic_quarter_evidence`(포크 #6). ydc `evidence_comments.py` 승격분이고 `topic_quarter_judgement` 의 기본키 여덟 칸이 그대로 이 표의 키이자 FK 다 — 판정이 지표 행에서 행 하나를 만드는 **파생**이라면 근거는 그 셀을 만든 문서를 도로 가리키는 **포인터**라, 본문을 베끼지 않고 `corpus_document` 를 FK 로 든다. 셀에서 원문까지 손으로 조인하지 않고 닿는 길은 뷰 `db/views/topic_quarter_evidence_quote.sql` 이고(이 이슈의 완료 기준), 근거가 정말 그 셀의 것인지는 `db/views/topic_quarter_evidence_violation.sql` 이 되묻는다. 선별 규칙 넷과 `cosmai retrieval search` 로 대체하지 않는 근거(실측 포함)는 `interfaces.md` §근거 가 지고, 값은 `analysis/evidence`(`cosmai trend evidence`)가 넣는다. 그 위에서 규칙이 유형을 정하는 기회 카드(`cosmai trend cards`, ydc `cards.py`)는 **표를 만들지 않는다** — 같은 계약의 §기회 카드 | `tests/test_evidence_contract.py` (포인터·모집단 동치·자리 사다리·불변식 뷰) + `tests/test_evidence_rules.py` (선별 규칙 넷) + `tests/test_evidence_pipeline.py` (읽기·적재·멱등) + `tests/test_cards_rules.py` (유형 배정과 어휘 여섯) + `tests/test_evidence_numbers.py` (계약이 인용한 픽스처 수치와 `tool/measure-evidence-fixture` 의 재측정·DB 산출 삼자 대조) + `tests/test_evidence_golden.py` (ydc `evidence_comments.py`·`cards.py` 와 대조) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/*.sql` | (the same directory as above) | one application path, `db/migrate.sh` (shared by production and tests); what has been applied is recorded in the ledger `needs.schema_migration` |
| `interfaces.md` §민감도 | 민감도·후향 검증 셋(포크 #41)의 세 반사실 모집단과 상수. ydc `panel_sensitivity.py`·`backtest.py`·`spam_ad_flags.py` 승격분이고 **어느 표에도 쓰지 않는다** — 반사실 행에는 022 의 `panel_role` 어휘에도 `analysis_run` 에도 자리가 없어서다. 답은 `cosmai trend sensitivity` 의 stdout 이다 | `tests/test_sensitivity_rules.py` (창 유도·판정 대상 게이트·세 적중 기준·버린 정규식·묶음 단위 제외) + `tests/test_sensitivity_golden.py` (ydc 세 스크립트와 1:1) + `tests/test_sensitivity_pipeline.py` (막힘·종료 코드·저장된 표가 그대로인가) |
| `interfaces.md` §질의 라우팅 | 질의 라우터를 **붙이지 않는다**는 결정과 그 근거(포크 #47). 성분명 판정의 정본이 토크나이저 사전이 아님 · 레포의 성분명 후보 셋이 전부 담론어를 담는다 · 신호 넷 중 둘(성분명·등록번호)의 원천이 없어 갈래 둘이 막힌다(upstream `slopindustries/cosmai#73` · 포크 #55) · 남은 신호 둘만 보는 라우터는 실측으로 이득 0 · 손해 2 · 우리 사전 위의 오라우팅 실측. 어느 표에도 쓰지 않는다 — 코드가 늘지 않는 결정이다 | `tests/retrieval/test_query_routing.py` (사전이 성분명 목록이 아니다 · 후보 셋의 오염 · `tool/measure-query-routing` 의 재측정과 계약 표의 대조) |
| `interfaces.md` §대조 | 네 소스(유튜브 댓글·자막·커머스 리뷰·커머스 랭킹)를 나란히 놓는 세 답과 상수. ydc `source_composition.py`·`commerce_crosscheck.py`·`cross_source.py` 승격분이고 **어느 표에도 쓰지 않는다** — 이 답의 행은 (주제)나 (성분) 하나가 키인데 022 의 분기 입자는 여덟 칸이 키이고 커머스 쪽에는 분기도 명부도 없다. 그래서 이 승격의 DDL 은 0장이다. 답은 `cosmai trend crosscheck` 의 stdout 이다. 같은 절이 성분 키 오매칭 정정(`시카`→트라이에톡시카프릴릴실레인)과 `FORMULA_HOLD`·`PAPER_HOLD` 의 근거를 진다. ydc `commerce_ranking.py` 는 `analysis/aggregate/ranking.py` 와 겹쳐 승격하지 않는다(처분 보류) | `tests/test_crosscheck_rules.py` (구성 정의·극성·긍정률·해석 다섯) + `tests/test_crosscheck_keys.py` (고친 키·버린 별칭 셋·성분표 쪼개기) + `tests/test_crosscheck_pipeline.py` (읽기·막힘·종료 코드·아무것도 안 쓴다) + `tool/measure-crosscheck-keys` (키가 지금 표에서 무엇을 잡는지 다시 재어 `analysis/crosscheck/audit/known_names_v1.csv` 와 맞댄다 — CI 는 운영 표에 못 닿는다). 전량 1:1 은 CI 가 못 한다 — ydc 의 성분표 CSV 가 그 레포에 있어서다. 절차와 대조 코드는 `tool/compare-ydc-crosscheck` 한 자리이고 2026-08-27 실행 **차이 0** 이다 |
| `interfaces.md` §홀드아웃 | 새로 쌓인 커머스 리뷰로 기존 결론을 되묻는 홀드아웃 검증과 상수(포크 #51). ydc `holdout_commerce.py` 승격분이고 **어느 표에도 쓰지 않는다** — 이 답의 행은 (팔, 주제)가 키인데 `팔` 의 경계가 청크 색인이라 `retrieval chunk` 가 돌 때마다 움직인다(오늘의 홀드아웃이 내일의 기존이다). 그래서 이 승격의 DDL 도 0장이다. 답은 `cosmai trend holdout` 의 stdout 이다. 전량 실측(2026-08-27, `verdict=순위 변동`)이 같은 절에 있고, **구성비 축의 재현 수를 싣지 않는 근거**(`share == rate / scale`, 계수 1.42·1.34 가 문턱의 눈금을 바꾼다)도 그 자리에 실측 세 줄과 함께 있다. 같은 절이 **컷오프를 날짜에서 청크 색인으로 바꾼 근거**와, ydc 가 손으로 얹은 추출 규칙 셋(정지·전순서 정렬·행수 대조)이 우리 자리에서 각각 무엇이 지는지를 든다 — 그대로 옮기면 항등식이라 검사가 아니다 | `tests/test_holdout_rules.py` (두 분모·축의 눈금·순위 게이트·표준화·바스켓·판정 네 갈래·상수 표 대조) + `tests/test_holdout_pipeline.py` (두 팔의 가름·한 스냅샷·막힘 넷·종료 코드·아무것도 안 쓴다) + `tool/compare-ydc-holdout` (태그의 `holdout_commerce.py` 를 손대지 않고 돌려 상수 셋·규칙 넷을 맞댄다 — 컷오프·BASE·플랫폼 목록은 그 머신의 배치라 맞대지 않고 도구가 말하고 넘어간다) |
| `entrypoints.md` | CLI entry points · exit codes · the shared run/fetch_log view | CLI `--help` snapshot + a view-exists test |
| `interfaces.md` | the input and output types and formulas of the four analysis packages, and the baselines the evaluation harness has to clear | `tests/test_contract_types.py` compares against `analysis/types.py` · `eval <task>` compares against the baselines |
| `formats.md` | 사전·평가셋 CSV/테이블 포맷 · §카테고리 표기(A21) 가 `category` 의 정본을 못박는다 | 적재 스크립트가 검증 · `tests/test_category_canonical.py` 가 §카테고리 표기의 python 블록을 `analysis/units.py` 와 대조 |
| `anon_exposure.md` | the relations PostgREST `postgrest_anon` reads — **34 before the application** (measured 2026-08-27) and **21 after** (#168 option B, not applied) | `tests/test_anon_exposure_contract.py` compares the `needs` section against a real database's `has_table_privilege` and the after-application section against `db/grants/postgrest_anon_old_stack.sql` · the production measurement is `db/grants/postgrest_anon_check.sql` |
| `secrets.md` | the secret file's path and the key names (no values) | key-existence check at start-up |
| `versioning.md` | the `*_version` column rules and the keys of `analysis_run.versions` | `tests/test_version_strings.py` (the two formats) + `tests/test_trend_quarter.py` (the `metric` key) + code review |
| `../db/grants/needs_runtime_reader.sql` | SELECT on the 12 source tables the analysis reads | `tests/test_grants_reader.py` compares against `ddl/current` |

Principle: an interface is fixed here first, and the behaviour is implemented and verified step by step inside the package. A contract change arrives as one PR carrying contract, implementation and tests together.

## 001 주석 정정 (001 은 수정하지 않는다 — 추가만이 사전 승인 범위다)
- `001:8` "needs_reader: SELECT" — there is no such role (T12). Reading is the PostgREST `postgrest_anon` whitelist (`db/grants/postgrest_anon_needs.sql`) and the analysis role `needs_runtime` (`db/grants/needs_runtime_reader.sql`), and nothing else.
- `001:74` the `aspect` in the `labeled_set.task` comment — dropped for want of both an evaluation set and a baseline (B11). The current list is in `entrypoints.md`.
- `001:156` `low_complete` 주석 "RATING_ASC 표본에 3★이 섞임" — 실제 규칙은 `(low_collected < 150) or has_3star` 다(T5). `formats.md §표본 상수`.
- `001:105` `UNIQUE (src, ref, need_key, sentence)` — 005 tore it up, so it is false now. The current natural key is `UNIQUE INDEX (src, ref, need_key, extractor_version, md5(sentence))`. `ddl/needs/005_need_mention_natural_key.sql`.
