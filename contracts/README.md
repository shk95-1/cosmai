# contracts — only what a machine can check

This directory is not a design document. Everything here has to be in **a shape a test or a script can compare against**.

| file | content | how it is checked |
|---|---|---|
| `ddl/current/*.sql` | the actual schema dump of the DB as of 2026-08-23 (app.trend_radar 13 · app.tubedepth 13 · cosmai.cosmai 12 tables). **These table shapes must hold** even after the collectors are ported — except `cosmai.cosmai.sql`, which since 2026-09-04 (#18) is the record of the dropped database `cosmai`, not a live target | re-dump with `pg_dump --schema-only` → diff |
| `ddl/current/app.trend_radar.sql` + `ddl/tubedepth/*.sql` | **the canonical form of the two collector schemas is the dump plus the additive files, in that order** — the baseline is never re-dumped and never edited, and every change to these schemas from now on is one more `ddl/<schema>/NNN_*.sql`. Three paths compose exactly that and nothing else: `db/migrate.sh` step (0) (which builds them when they are absent and skips when they are there, so production always skips), `tests/conftest.py`'s throwaway schemas, and `tool/checks/ddl-drift`'s expected state. `alembic_version` comes with the baseline, empty, and stays that way: the old repos' alembic does not run again | `tests/test_empty_db_bootstrap.py` (the empty database, the skip, and the deploy against the fixture) + `tool/checks/ddl-drift` |
| `ddl/needs/001_needs.sql` | the new `app.needs` schema (17 tables). Three comments are stale — see the footnote below | a `pg_catalog` comparison test after the migration is applied |
| `ddl/needs/002_audit_additive.sql` | the **additions only** the 2026-08-23 contract audit (issue #17) asked for — 3 tables (`need_key`, `category_map`, `product_ref_candidate`) + 31 columns. 20 tables in total | the same test + `tests/test_ddl_additive_only.py` |
| `ddl/needs/005_need_mention_natural_key.sql` | the `need_mention` natural-key replacement — it solves the btree 2704B ceiling (#5, a production failure) and the seed/analysis collision (#12 option A) at once. `UNIQUE (src, ref, need_key, sentence)` → `UNIQUE INDEX (src, ref, need_key, extractor_version, md5(sentence))`. **The one exception to additive-only** (user approval 2026-08-24) | the `SANCTIONED_DESTRUCTIVE` whitelist in `tests/test_ddl_additive_only.py` + `tests/test_need_mention_natural_key.py` |
| `ddl/needs/020_retrieval_chunk.sql` | the retrieval unit's (#28) chunk store `needs.retrieval_chunk` — the 5-column chunk contract (`analysis/retrieval/chunks.py` FIELDS) + 2 derived columns (`text_md5`·`chunked_at`). The number 020 is the long-lived branch `feat/ydc-import`'s block, so file names never collide with main's 00N | `tests/retrieval/test_contract.py` (columns · NOT NULL · GRANT · number block) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/021_aspect_lexicon_extra.sql` | `aspect_lexicon.extra` (jsonb) — the column holding facts that differ per ruleset. The retrieval unit's topic dictionary (`ruleset='retrieval-topic'`, fork #8) carries an alias's script family, the topic type and whether it is used in trends here. Existing rows stay `{}` (additive only) | `tests/retrieval/test_topics.py` (load · active · equivalence) + `tests/retrieval/test_lexicon_v3.py` (the v3 ledger against the loaded source) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/022_panel_and_quarter.sql` | the panel population `needs.panel_roster`+`needs.panel_channel` (the place for the 43-channel roster — the values are put in from `eval/panel/channels_v1.csv` by `db/seed/panel.py`, fork #31) and the canonical table for the quarterly grain `needs.metrics_topic_quarter` (it **coexists** with the month; which table carries which grain is `formats.md` §Time). Both tables leave on the row which population the ratio is against, and that pointer names the version table by FK. The topic axis is `topic_key` — its registry is `aspect_lexicon(ruleset='retrieval-topic')`, a different axis from `needs.need_key`. The row set's two invariants (a dense grid · a closed denominator) are asked back by the view `db/views/metrics_topic_quarter_violation.sql`. The values are put in by `analysis/trend` (`cosmai trend quarter`, fork #5) from the active corpus snapshot | `tests/test_panel_quarter_contract.py` (canonical table · roster · denominator · role vocabulary · `quarter` grammar · the two axes · decimal places · the invariant view) + `tests/test_trend_quarter.py` (the five formulas) + `tests/test_trend_pipeline.py` (the population; the view is empty after a load) + `tests/test_trend_golden.py` (1:1 with ydc v0.2 trend.py's output) + `tests/test_month_grain_regression.py` (the month output is unchanged) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/023_corpus_snapshot.sql` | the place for the 2026-08-19 YouTube corpus snapshot: `needs.corpus_snapshot`+`corpus_document`+`corpus_mention` (fork #4). The 261,317 documents and 105,358 mentions of the three ydc handover CSVs live here — the values are put in from `archive/yt-handoff/` by `db/corpus` (`python -m db.corpus load <dir>`). **The observation version stands at the front of the unique key**, so a re-collection (#38) arriving under the same `source + source_item_id` cannot overwrite the old observation. The channels do not become a table and are only compared against the active `panel_channel` roster (a denominator lives in one table alone). The place of the rule and limitation sentences is `formats.md` §Corpus snapshot · `interfaces.md` §Limitations of the population, and the row counts the manifest declares are compared by the loader against the import and refused before switching on if they differ — on a new version the inserted row count is compared too, so `input_counts.duplicate_docs = 0` stands as well (fork #37) | `tests/test_corpus_import.py` (import · idempotence · nothing overwritten · the three reproduction numbers · the declared row counts · the duplicate unique key · the contract sentences) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/024_topic_quarter_judgement.sql` | the place for the seven trend-type verdicts and the two scores, `needs.topic_quarter_judgement` (fork #40). It is the promotion of ydc `judge.py`, and the eight primary-key columns of `metrics_topic_quarter` are this table's key and FK as they are — **a derivation rather than an aggregate**, so its name does not start with `metrics_` and it has no line in the grain table of `formats.md` §Time. The grounds for the five verdict constants and the adopt-or-refit judgment are carried by the table of `interfaces.md` §Verdict, and the values are put in by `analysis/judge` (`cosmai trend judge`). The 1:1 with the metric rows and the agreement of `gap_pp` across the two rows are asked back by the view `db/views/topic_quarter_judgement_violation.sql` | `tests/test_judgement_contract.py` (derivation · vocabulary · decimal places · the closed reason set · the invariant view) + `tests/test_judge_constants.py` (the constants are the same numbers as the contract table) + `tests/test_judge_rules.py` (the verdict order and the two scores) + `tests/test_judge_golden.py` (1:1 with ydc v0.2 judge.py's output) + `tests/test_judge_pipeline.py` (read · load · idempotence) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/025_topic_quarter_evidence.sql` | the place for the consumer speech that holds up a verdict cell, `needs.topic_quarter_evidence` (fork #6). It is the promotion of ydc `evidence_comments.py`, and the eight primary-key columns of `topic_quarter_judgement` are this table's key and FK as they are — if the verdict is a **derivation** that makes one row out of a metric row, the evidence is a **pointer** back at the documents that made that cell, so it does not copy the body but names `corpus_document` by FK. The path from a cell to the original text without joining by hand is the view `db/views/topic_quarter_evidence_quote.sql` (this issue's completion criterion), and whether the evidence really belongs to that cell is asked back by `db/views/topic_quarter_evidence_violation.sql`. The four selection rules and the grounds for not replacing it with `cosmai retrieval search` (measurement included) are carried by `interfaces.md` §Evidence, and the values are put in by `analysis/evidence` (`cosmai trend evidence`). The opportunity cards on top of it, whose types the rules decide (`cosmai trend cards`, ydc `cards.py`), **make no table** — §Opportunity cards of the same contract | `tests/test_evidence_contract.py` (pointer · population equivalence · the slot ladder · the invariant view) + `tests/test_evidence_rules.py` (the four selection rules) + `tests/test_evidence_pipeline.py` (read · load · idempotence) + `tests/test_cards_rules.py` (type assignment and the six-name vocabulary) + `tests/test_evidence_numbers.py` (the three-way comparison of the fixture numbers the contract quotes, `tool/measure-evidence-fixture`'s re-measurement and the DB output) + `tests/test_evidence_golden.py` (against ydc `evidence_comments.py`·`cards.py`) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/026_retrieval_ask_log.sql` | the answer layer's query log `needs.retrieval_ask_log` (fork #73): one row per real LLM call of `cosmai retrieval ask` — query, engine, gate verdict, per-token chunk frequency, the folded document ids, the index fingerprint, the lexicon and store stamps, model and cost. Written after the ledger is settled, outside the LLM round trip; nothing on `--dry-run` or when there was no evidence. The columns exist so the next judgments (BM25's partial answers on the query axis, the real query distribution behind fork #11) can be measured instead of guessed — `interfaces.md` §Answer layer | `tests/retrieval/test_ask.py` (no-call paths, folding, the row and its fingerprint, budget before the call, truncation refused) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/027_panel_single_active.sql` | the DB-side guard that `needs.panel_channel` never commits with two active versions (fork #34): a deferrable constraint trigger checked at commit, so the loader's insert-then-activate swap inside one transaction passes and a hand `UPDATE` that leaves two versions active is refused with `check_violation`. 022's partial index constrains one row at a time; this is the set condition it cannot express | `tests/test_panel_seed.py` (the rejected hand UPDATE, the swap's mid-transaction state) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/028_mfds_registration.sql` | the MFDS cosmetic registration ledger as a reference table (fork #55): `needs.mfds_snapshot` (one row per load — origin tag, source file, row count, newest report date, `update_policy`, `loaded_at`) + `needs.mfds_registration` (4,735 filings). `report_seq` alone is the key, the opposite of 023 on purpose: a filed report is closed history, so a second snapshot must not enter the same registration twice, while a re-observation of a video genuinely is a different fact. Values come from `eval/mfds/mfds_items_v1.csv` via `db/seed/mfds.py` (`uv run python -m db.seed --only mfds`, run from the upstream checkout after the wave PR). The join to our products is `entp_key` against `needs.entity_lexicon(kind='brand').surface`, both sides through the same fold (233 of 4,735 filings on 40 brands when measured 2026-09-04; `uv run tool/measure-mfds-join` is the live count); **not updated**, and a refresh is a reviewed snapshot bump, never a silent merge — the sentences are in `formats.md`, MFDS registration ledger section | `tests/test_mfds_seed.py` (row count · key uniqueness · idempotent rerun · the snapshot row's facts · the stored key equals the pure function · the FK · the SQL join) + `tool/measure-mfds-join` (re-measures the join against the database as it is now) + `tests/test_ddl_additive_only.py` |
| `ddl/needs/*.sql` | (the same directory as above) | one application path, `db/migrate.sh` (shared by production and tests); what has been applied is recorded in the ledger `needs.schema_migration` |
| `interfaces.md` §Sensitivity | the three counterfactual populations and the constants of the sensitivity and backtest trio (fork #41). It is the promotion of ydc `panel_sensitivity.py`·`backtest.py`·`spam_ad_flags.py` and **writes to no table** — a counterfactual row has no place in 022's `panel_role` vocabulary or in `analysis_run`. The answer is the stdout of `cosmai trend sensitivity` | `tests/test_sensitivity_rules.py` (window derivation · the verdict-target gate · the three hit bases · the discarded regexes · bundle-grain exclusion) + `tests/test_sensitivity_golden.py` (1:1 with ydc's three scripts) + `tests/test_sensitivity_pipeline.py` (blocks · exit codes · are the stored tables unchanged) |
| `interfaces.md` §Query routing | the decision **not to attach** a query router, and its grounds (fork #47). The canonical decision on an ingredient name is not the tokeniser dictionary · every ingredient-name candidate set in the repo holds discourse words · two of the four signals (ingredient name, registration number) have no source so two branches are blocked (upstream `slopindustries/cosmai#73` · fork #55) · a router that sees only the remaining two signals measured gain 0 and loss 2 · the misrouting measurement on our own dictionary. It writes to no table — it is a decision that adds no code | `tests/retrieval/test_query_routing.py` (the dictionary is not an ingredient-name list · the candidate sets' contamination · `tool/measure-query-routing`'s re-measurement against the contract table) |
| `interfaces.md` §대조 | 네 소스(유튜브 댓글·자막·커머스 리뷰·커머스 랭킹)를 나란히 놓는 세 답과 상수. ydc `source_composition.py`·`commerce_crosscheck.py`·`cross_source.py` 승격분이고 **어느 표에도 쓰지 않는다** — 이 답의 행은 (주제)나 (성분) 하나가 키인데 022 의 분기 입자는 여덟 칸이 키이고 커머스 쪽에는 분기도 명부도 없다. 그래서 이 승격의 DDL 은 0장이다. 답은 `cosmai trend crosscheck` 의 stdout 이다. 같은 절이 성분 키 오매칭 정정(`시카`→트라이에톡시카프릴릴실레인)과 `FORMULA_HOLD`·`PAPER_HOLD` 의 근거를 진다. ydc `commerce_ranking.py` 는 `analysis/aggregate/ranking.py` 와 겹쳐 승격하지 않는다(처분 보류) | `tests/test_crosscheck_rules.py` (구성 정의·극성·긍정률·해석 다섯) + `tests/test_crosscheck_keys.py` (고친 키·버린 별칭 셋·성분표 쪼개기) + `tests/test_crosscheck_pipeline.py` (읽기·막힘·종료 코드·아무것도 안 쓴다) + `tool/measure-crosscheck-keys` (키가 지금 표에서 무엇을 잡는지 다시 재어 `analysis/crosscheck/audit/known_names_v1.csv` 와 맞댄다 — CI 는 운영 표에 못 닿는다). 전량 1:1 은 CI 가 못 한다 — ydc 의 성분표 CSV 가 그 레포에 있어서다. 절차와 대조 코드는 `tool/compare-ydc-crosscheck` 한 자리이고 2026-08-27 실행 **차이 0** 이다 |
| `interfaces.md` §홀드아웃 | 새로 쌓인 커머스 리뷰로 기존 결론을 되묻는 홀드아웃 검증과 상수(포크 #51). ydc `holdout_commerce.py` 승격분이고 **어느 표에도 쓰지 않는다** — 이 답의 행은 (팔, 주제)가 키인데 `팔` 의 경계가 청크 색인이라 `retrieval chunk` 가 돌 때마다 움직인다(오늘의 홀드아웃이 내일의 기존이다). 그래서 이 승격의 DDL 도 0장이다. 답은 `cosmai trend holdout` 의 stdout 이다. 전량 실측(2026-08-27, `verdict=순위 변동`)이 같은 절에 있고, **구성비 축의 재현 수를 싣지 않는 근거**(`share == rate / scale`, 계수 1.42·1.34 가 문턱의 눈금을 바꾼다)도 그 자리에 실측 세 줄과 함께 있다. 같은 절이 **컷오프를 날짜에서 청크 색인으로 바꾼 근거**와, ydc 가 손으로 얹은 추출 규칙 셋(정지·전순서 정렬·행수 대조)이 우리 자리에서 각각 무엇이 지는지를 든다 — 그대로 옮기면 항등식이라 검사가 아니다 | `tests/test_holdout_rules.py` (두 분모·축의 눈금·순위 게이트·표준화·바스켓·판정 네 갈래·상수 표 대조) + `tests/test_holdout_pipeline.py` (두 팔의 가름·한 스냅샷·막힘 넷·종료 코드·아무것도 안 쓴다) + `tool/compare-ydc-holdout` (태그의 `holdout_commerce.py` 를 손대지 않고 돌려 상수 셋·규칙 넷을 맞댄다 — 컷오프·BASE·플랫폼 목록은 그 머신의 배치라 맞대지 않고 도구가 말하고 넘어간다) |
| `entrypoints.md` | CLI entry points · exit codes · the shared run/fetch_log view | CLI `--help` snapshot + a view-exists test |
| `interfaces.md` | the input and output types and formulas of the four analysis packages, and the baselines the evaluation harness has to clear | `tests/test_contract_types.py` compares against `analysis/types.py` · `eval <task>` compares against the baselines |
| `formats.md` | the CSV and table formats of the dictionaries and evaluation sets · §Category notation (A21) nails down the canonical form of `category` | the loader scripts validate them · `tests/test_category_canonical.py` compares §Category notation's python block against `analysis/units.py` |
| `anon_exposure.md` | the relations PostgREST `postgrest_anon` reads — **34 before the application** (measured 2026-08-27) and **21 after** (#168 option B, not applied) | `tests/test_anon_exposure_contract.py` compares the `needs` section against a real database's `has_table_privilege` and the after-application section against `db/grants/postgrest_anon_old_stack.sql` · the production measurement is `db/grants/postgrest_anon_check.sql` |
| `ownership.md` | What the fork may change, must not change, and must send upstream (#192 methodology row 3, incidents #150 · #103 · #115) — the fork-owned and must-not-change lists plus the shared surfaces PR #59 touched | `tests/test_ownership.py` (the needs DDL split at 020, no path on both lists, every listed path exists) + `tool/checks/ownership <upstream-ref>` before a fork → upstream PR + the `tool/issue audit` item |
| `section-names.md` | the rename ledger for every `§` anchor: the Korean name each section carried before #206 part 2, and the English name it carries now | read by a person following an older issue; the English names are checked by `tests/` through the sections they name |
| `secrets.md` | the secret file's path and the key names (no values) | key-existence check at start-up |
| `versioning.md` | the `*_version` column rules and the keys of `analysis_run.versions` | `tests/test_version_strings.py` (the two formats) + `tests/test_trend_quarter.py` (the `metric` key) + code review |
| `../db/grants/needs_runtime_reader.sql` | SELECT on the 12 source tables the analysis reads | `tests/test_grants_reader.py` compares against `ddl/current` |

Principle: an interface is fixed here first, and the behaviour is implemented and verified step by step inside the package. A contract change arrives as one PR carrying contract, implementation and tests together.

## 001 comment corrections (001 is not edited — only additions are within the pre-approved scope)
- `001:8` "needs_reader: SELECT" — there is no such role (T12). Reading is the PostgREST `postgrest_anon` whitelist (`db/grants/postgrest_anon_needs.sql`) and the analysis role `needs_runtime` (`db/grants/needs_runtime_reader.sql`), and nothing else.
- `001:74` the `aspect` in the `labeled_set.task` comment — dropped for want of both an evaluation set and a baseline (B11). The current list is in `entrypoints.md`.
- `001:156` the `low_complete` comment "a 3★ mixed into the RATING_ASC sample" — the real rule is `(low_collected < 150) or has_3star` (T5). `formats.md §Sample constants`.
- `001:105` `UNIQUE (src, ref, need_key, sentence)` — 005 tore it up, so it is false now. The current natural key is `UNIQUE INDEX (src, ref, need_key, extractor_version, md5(sentence))`. `ddl/needs/005_need_mention_natural_key.sql`.
