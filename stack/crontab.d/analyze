# UTC. analyze all is DB-only (no external fetch), so contracts/entrypoints.md §Schedule exempts it from
# the gap rule the commerce walks follow (the analyze lock sets a different one -- see the owner-pass
# note below) -- which is also why it is not in the commerce container:
# nothing it does competes for a host budget, and a commerce collector stopped for a rollback must not
# take the analysis of already-collected rows down with it.
#
# 이 줄에는 --impl 이 없으니 규칙 극성으로 돈다. 무엇을 다시 라벨할지는 이제 이 줄의 인자가 아니라
# analysis/polarity/ownership.py 의 소유 표가 정한다 (#31, #97): 주인이 있는 (lexicon_category, 달)의
# 행 -- 선블록의 모든 달(since=ALWAYS)과 나머지 26개의 2026-08 이후 -- 은 지우지도 덮지도 않고, 주인
# 없는 카테고리와 주인의 since 앞의 달, lexicon_category 가 없는 행(유튜브 댓글·카테고리를 못 붙인
# 리뷰)이 매일 새로 라벨된다. 소유를 옮기는 곳은 그 파일 하나다 -- 크론 인자로 옮기면 이 줄과 아래 줄이
# 서로 어긋나도 아무도 모른다.
0 5 * * *   cosmai analyze all
#
# Suspended 2026-09-06 (#242): the `0 8` gemma4 line that used to stand here is gone. The Tailscale GPU
# host it depended on no longer answers /api/tags, and this host's own ollama is CPU-only with too little
# memory for the 9.2 GiB Q4_K_M weights -- a local CPU-only ollama cannot carry the ~2,800 calls/day steady
# state either way (the issue's evaluation comment has the measurements). `OWNERS` in
# analysis/polarity/ownership.py holds no gemma4 scope while this line is absent -- a registration with no
# line is a hole, and a line with no registration is an empty run, so the two only ever move together.
# To re-enable: a model host that answers /api/tags, OLLAMA_URL pointed at it, the ownership entries back in
# OWNERS (the 26 with CRON_SINCE, the sunblock block with ALWAYS), and this line back -- all in one PR.
#
# The commentary below (T, the GPU window, the two-step procedure) is kept for the day the line returns.
#
# **왜 0 8 인가 -- 이 명령의 T 는 아직 미측정이다.**
# 규칙은 그대로다: 인접한 두 줄의 간격이 앞 줄의 **최악** 소요 T 보다 넓어야 한다. #16 의 analyze 락은
# 기다리지 않는다 -- 뒤에 온 실행은 그 밤을 통째로 건너뛰고 partial(1)로 끝난다 (commerce 줄과 같은
# 모양의 규칙이다, contracts/entrypoints.md §스케줄). 실측이 있는 T 는 전량 패스의 것 하나뿐이다:
# 선블록 하나를 도는 run 16 이 2026-08-24 23:12 에 시작해 08-25 05:57 에 ok 로 끝났다 -- **6h44m**.
# gemma4 가 규칙보다 45% 많은 문장에 aspect 를 붙이는 것이 그 시간의 정체다.
# **그런데 이 줄이 돌리는 것은 전량이 아니라 `--missing`(#98)이고 그 T 는 아무도 재지 않았다** --
# 운영 실행은 조정자의 몫이라 이 줄을 넣은 작업이 잴 수 없었다. 그래서 시각은 T 를 아는 대신 간격을
# 최대로 벌려 골랐다:
#     05:00 → 08:00   3h    analyze all 에 주는 몫. 규칙 극성은 정규식이라 LLM 패스와 자릿수가 다르고,
#                           설령 넘겨도 손해를 보는 것은 락을 못 잡는 이 줄이지 05:00 이 아니다.
#     08:00 → 05:00   21h   이 줄에 주어진 간격. T 가 21h 를 넘는 밤에만 05:00 이 skip 된다 -- run 16
#                           (6h44m, 전량, 선블록 하나)의 3배이고, 코퍼스 전량 35k 언급을 그 실측 속도
#                           (약 1.75s/문장)로 통째로 다시 판정해도 그 안에 든다.
# 첫 밤만 다르다: 26개의 since 가 2026-08 이라(analysis/polarity/ownership.py) 그 달의 밀린 리뷰가 한
# 번에 든다. 그 뒤의 밤은 그날 새로 들어온 것뿐이다 -- 고르는 기준이 날짜가 아니라 "내 판본 행이 없는
# 원천 행"이라 늦게 도착한 리뷰도 그 하루에 잡힌다 (#98). **조정자가 두 T(첫 밤·정상)를 재고, 21h 를
# 위협하면 이 시각을 옮긴다.** 간격이 모자랄 때 고칠 곳은 락이 아니라 간격이다 -- 두 줄을 벌리거나
# 05:00 을 뒤로 민다. 넘긴 밤의 analyze all 은 skip 되고, 모든 단계가 자연키 upsert 라 다음 05:00 이
# 그 밤의 몫까지 그대로 가져간다.
#
# **GPU window: 08:00-16:00 UTC (17:00-01:00 KST) -- void while the `0 8` line is absent (#242).** There
# is one GPU, and overlapping with another repo's (the fork's) `cosmai retrieval embed` (380k chunks, 20.6
# minutes idle) stretches to 5h38m -- both sides pay the same cost (measured in #28). An advisory lock
# cannot see another process, so the window is fixed by documentation instead: `embed` is run to avoid this
# window. The 8h width is not a T, it is an **upper-bound estimate** -- margin added on top of the only
# measured value (the full pass's 6h44m), and once T is pinned down this width narrows along with it. The
# same sentence also appears in stack/README.md. The fork's `retrieval embed` has nothing to avoid until the
# `0 8` line comes back.
#
# Times are UTC (this file's own convention, and compose pins TZ=UTC). Moving the timezone to KST is
# #36, and moving the whole file over in one go is #89 -- this line follows the file's current
# convention. Because the constraint is a **relative** gap to 05:00, moving both lines together leaves
# nothing to recompute.
#
# Handing one more category to gemma4 is still a two-step procedure, but the two steps are no longer
# glued together (#97) -- applies once the `0 8` line above is back (#242):
#   1) Add that lexicon_category, gemma4's version, and the first month the owner will be responsible
#      for (since) to OWNERS in analysis/polarity/ownership.py, and deploy it. Writing since as next
#      month leaves the month before it kept current by the 05:00 line above, so registering it and
#      deferring the pass never freezes that category -- that is why all 26 could go up at once without
#      waiting on a 6h44m full pass.
#   2) Months from since on are filled in every night by the 08:00 line above. Only the historical
#      correction (handing the months before since to the owner) is left as a manual command:
#      cosmai analyze polarity --impl ollama:gemma4:latest [--scope <that category>].
#      --scope now only narrows -- omitting it makes the owner run its entire (scope, span).
# Reversed (the pass runs before the registration), the pass still finishes successfully and the 05:00
# line above overwrites that label with the rule again that night -- because a scope with no owner is
# this line's share. To make that ordering impossible to get wrong, the CLI rejects a --scope with no
# 1) done first (cosmai/cli.py -> unready in analysis/polarity/ownership.py).
