trend-radar(archived) 의 `feat/review-low` 브랜치(b9ffa95)를 패치로 보존. 원격은 archive 되어 푸시 불가.
`collectors/commerce/` 로 trend-radar `src/` 를 이식할 때 이 패치를 함께 적용한다 (review_low 데이터셋: suncare 상위 10 × ASC 6p + DESC 1p + stats, 1,172 테스트 통과본).
