Preserves trend-radar(archived)'s `feat/review-low` branch (b9ffa95) as a patch; the remote is archived so it cannot be pushed to.
Apply this patch together when porting trend-radar's `src/` into `collectors/commerce/` (the review_low dataset: suncare top 10 × ASC 6p + DESC 1p + stats, the build that passed 1,172 tests).
